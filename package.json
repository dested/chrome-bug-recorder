{
  "name": "gripe",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "description": "Rant at your app, get a bug report. Point at what is broken, say what is wrong, hand the markdown to your coding agent.",
  "scripts": {
    "icons": "node scripts/make-icons.mjs",
    "ort": "node scripts/copy-ort.mjs",
    "build": "npm run icons && npm run ort && vite build && vite build --config vite.content.config.ts && node scripts/prune-dist.mjs",
    "dev": "npm run icons && npm run ort && vite build --watch",
    "preview": "node scripts/preview.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
